# MiSans Support For AOSP
为支持自定义字体的 AOSP 系统增加 MiSans 支持。\
并支持多档字重（100-900） 。

在 Xiaomi 14 DerpFest Android16 QPR0 上测试通过。

理论支持 Magisk、APatch、KernelSU，仅在 APatch 上测试通过。

安装此模块后，即可在 系统自定义-字体 中找到 MiSans 字体选项。
